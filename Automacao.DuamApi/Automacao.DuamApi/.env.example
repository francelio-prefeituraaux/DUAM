MYSQL_ROOT_PASSWORD=troque-esta-senha
MYSQL_PASSWORD=troque-esta-senha-tambem

# Origem do frontend autorizada a chamar a API via CORS (ex.: https://duam.suaprefeitura.gov.br)
FRONTEND_ORIGIN=

# Quantos workers do Hangfire processam jobs em paralelo (cada um pode abrir um Chrome headless)
WORKER_COUNT=2
